# Top-1 strategy: make Supermemory Local the star

## Judge problem (honest)

RECALL can look like “nice UI + importers + MCP on top of SM.”  
Supermemory judges reward: **engine intelligence visible** — hybrid search, profiles, extraction, local ownership.

## Positioning (say this)

> **Supermemory Local is the memory intelligence layer.**  
> RECALL is the product that fills it with real multi-AI history and injects it back into every surface (chat paste + MCP).

Not: “we built a memory system.”  
Yes: “we made Supermemory Local load-bearing and visible.”

## What SM already does for us (prove it live)

| SM capability | Where RECALL shows it |
|---------------|------------------------|
| Hybrid search (`/v4/search`) | **Search** page — similarity % + “SM hybrid” badge |
| Profile static + dynamic (`/v4/profile`) | **Home** — Supermemory intelligence panel |
| Store + embed docs | **Import** → Setup memory count rises |
| Local ownership | **Setup** — Connected · localhost:6767 |
| Same engine for agents | **Connect** MCP · `recall_search` / `recall_context` |

Custom SQLite graph = **thin structure** (types, supersession UI) on SM document IDs — not a second brain.

## Demo order (top-1 cut)

1. **Setup** — Supermemory Local Connected (engine first)  
2. **Import** both fixtures → count ticks up  
3. **Home** — open **Supermemory intelligence** profile (static + dynamic)  
4. **Search** “TypeScript backend” — show **~93% sim · SM hybrid** (Python + TS both high)  
5. **Conflicts** — cross-platform disagreement surfaced from that retrieval  
6. **Connect** — pack + MCP = inject *from the same local engine*

Skip: deep graph polish, extra importers, timeline.

## Scope discipline

| Do | Don’t |
|----|--------|
| One perfect path | New features mid-demo week |
| SM labels on every screen | Hide SM behind “RECALL knows” |
| Profile + hybrid search as hero | Custom graph as hero |
| MCP as thin client to SM | Claim we reimplemented memory |

## After video

Optional: hide demo-stats scoreboard (see landing HTML comment).
